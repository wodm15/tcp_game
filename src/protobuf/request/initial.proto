syntax = "proto3";

package initial;

// InitialPayload 구조
message InitialPayload {
  string deviceId = 1;     // deviceId (문자열)
  uint32 playerId = 2;     // playerId (uint32)
  float latency = 3;       // latency (float)
}
